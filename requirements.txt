# requirements.txt (runtime only)

openai>=1.35.0                  # o3‑pro & o3‑deep‑research
langgraph>=0.2.0               # orchestration + persistence
hyperliquid-python-sdk         # Hyperliquid DEX SDK
apexpro                        # ApeX Protocol SDK
ccxt                           # supplemental exchange data
pandas>=1.5.3,<2.0             # data frames (pins numpy <2.0)
numpy<2.0                      # avoid pandas–NumPy 2.0 conflict
python-dotenv>=1.0.0           # env var loader
